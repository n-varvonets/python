# помимо рексрсов есть еще памертьі, которіе динамически передавать в yaml файл
Parameters:
  # непосредственно при деплое .yaml file in cloudform укажем само значение
  BucketName:
    Type: String
    Description: 'A bucket name to set.'

Resources:
  S3Bucket:
    Type: AWS::S3::Bucket


# будет ошибка при деплое, т.к. нет разницьі между предудщем деплоим файла step-1-resource.yml... почему?
# потому что мьі нигде дальше не спользуем єтот параметр при создании ресурса